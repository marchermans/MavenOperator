---
# Generated from MavenOperator CRD entity classes.
# Group:   maven.operator.io
# Kind:    MavenRepository
# Version: v1alpha1
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: mavenrepositories.maven.operator.io
spec:
  group: maven.operator.io
  names:
    kind: MavenRepository
    listKind: MavenRepositoryList
    plural: mavenrepositories
    singular: mavenrepository
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              x-kubernetes-validations:
                # Hosted must have storage
                - rule: "self.type != 'Hosted' || has(self.storage)"
                  message: "spec.storage is required when type is Hosted"
                # Proxy must have upstream with a non-empty URL
                - rule: "self.type != 'Proxy' || (has(self.upstream) && self.upstream.url != '')"
                  message: "spec.upstream.url is required when type is Proxy"
                # Virtual must have at least one member
                - rule: "self.type != 'Virtual' || (has(self.virtual) && self.virtual.members.size() > 0)"
                  message: "spec.virtual.members must be non-empty when type is Virtual"
                # Authenticated download requires at least one basic user or CI trust binding.
                - rule: "self.auth.download.policy != 'Authenticated' || self.auth.download.users.size() > 0 || self.auth.download.ciTrust.size() > 0"
                  message: "spec.auth.download requires users or ciTrust when policy is Authenticated"
                # Authenticated upload requires at least one basic user or CI trust binding.
                - rule: "self.auth.upload.policy != 'Authenticated' || self.auth.upload.users.size() > 0 || self.auth.upload.ciTrust.size() > 0"
                  message: "spec.auth.upload requires users or ciTrust when policy is Authenticated"
              properties:
                type:
                  type: string
                  enum: [Hosted, Proxy, Virtual]
                  default: Hosted
                storage:
                  type: object
                  properties:
                    size:
                      type: string
                      default: "10Gi"
                    storageClassName:
                      type: string
                      nullable: true
                    accessMode:
                      type: string
                      enum: [ReadWriteOnce, ReadWriteMany, ReadWriteOncePod]
                      default: ReadWriteMany
                      description: >
                        PVC access mode. Defaults to ReadWriteMany so the import Job
                        can mount the PVC directly alongside the NGINX pod, and to
                        support future HA deployments with multiple replicas.
                    deletionPolicy:
                      type: string
                      enum: [Retain, Delete]
                      default: Retain
                upstream:
                  type: object
                  nullable: true
                  properties:
                    url:
                      type: string
                    cacheTtl:
                      type: string
                      default: "1d"
                    cachePvcSize:
                      type: string
                      nullable: true
                    auth:
                      type: object
                      nullable: true
                      properties:
                        secretRef:
                          type: string
                virtual:
                  type: object
                  nullable: true
                  properties:
                    members:
                      type: array
                      items:
                        type: string
                    metadataCacheTtlSeconds:
                      type: integer
                      default: 60
                auth:
                  type: object
                  properties:
                    download:
                      type: object
                      properties:
                        policy:
                          type: string
                          enum: [Anonymous, Authenticated]
                          default: Anonymous
                        users:
                          type: array
                          items:
                            type: object
                            properties:
                              secretRef:
                                type: string
                              role:
                                type: string
                                enum: [Reader, Deployer, Admin]
                                default: Deployer
                        ciTrust:
                          type: array
                          items:
                            type: object
                            properties:
                              platform:
                                type: string
                                enum: [GitHubActions, GitLab]
                                default: GitHubActions
                              issuerUrl:
                                type: string
                                nullable: true
                              audience:
                                type: string
                                nullable: true
                              role:
                                type: string
                                enum: [Reader, Deployer, Admin]
                                default: Deployer
                              claims:
                                type: object
                                additionalProperties:
                                  type: string
                        acls:
                          type: array
                          items:
                            type: object
                            properties:
                              path:
                                type: string
                              roles:
                                type: array
                                items:
                                  type: string
                                  enum: [Reader, Deployer, Admin]
                    upload:
                      type: object
                      properties:
                        policy:
                          type: string
                          enum: [Anonymous, Authenticated]
                          default: Authenticated
                        users:
                          type: array
                          items:
                            type: object
                            properties:
                              secretRef:
                                type: string
                              role:
                                type: string
                                enum: [Reader, Deployer, Admin]
                                default: Deployer
                        ciTrust:
                          type: array
                          items:
                            type: object
                            properties:
                              platform:
                                type: string
                                enum: [GitHubActions, GitLab]
                                default: GitHubActions
                              issuerUrl:
                                type: string
                                nullable: true
                              audience:
                                type: string
                                nullable: true
                              role:
                                type: string
                                enum: [Reader, Deployer, Admin]
                                default: Deployer
                              claims:
                                type: object
                                additionalProperties:
                                  type: string
                        acls:
                          type: array
                          items:
                            type: object
                            properties:
                              path:
                                type: string
                              roles:
                                type: array
                                items:
                                  type: string
                                  enum: [Reader, Deployer, Admin]
                ingress:
                  type: object
                  properties:
                    enabled:
                      type: boolean
                      default: false
                    host:
                      type: string
                      nullable: true
                    path:
                      type: string
                      nullable: true
                    tlsSecretRef:
                      type: string
                      nullable: true
                metrics:
                  type: object
                  properties:
                    enabled:
                      type: boolean
                      default: true
                      description: >
                        When true the operator injects nginx-prometheus-exporter
                        and mtail sidecars into every NGINX pod.
                    maxLabelCardinality:
                      type: integer
                      default: 10000
                    stubStatusPort:
                      type: integer
                      default: 9080
                    exporterPort:
                      type: integer
                      default: 9113
                    mtailPort:
                      type: integer
                      default: 3903
                    nginxExporterImage:
                      type: string
                      default: "nginx/nginx-prometheus-exporter:1.4"
                    mtailImage:
                      type: string
                      default: "ghcr.io/google/mtail:latest"
                    podMonitorEnabled:
                      type: boolean
                      default: false
                resources:
                  type: object
                  nullable: true
                  x-kubernetes-preserve-unknown-fields: true
            status:
              type: object
              x-kubernetes-preserve-unknown-fields: true
      additionalPrinterColumns:
        - name: Type
          type: string
          jsonPath: .spec.type
        - name: Phase
          type: string
          jsonPath: .status.phase
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp

